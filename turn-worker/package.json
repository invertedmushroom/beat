{
  "name": "beat-turn-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret:token-id": "wrangler secret put TURN_TOKEN_ID",
    "secret:api-token": "wrangler secret put TURN_API_TOKEN"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
